BifidCipherGUI
To compile just run the makefile file 

Things to do:
  Fix crash on exactly 24 and 40 characters
  Keep buttons from resizing to match the size of the text

Completed:
  Create a Makefile instead of using the build.sh
